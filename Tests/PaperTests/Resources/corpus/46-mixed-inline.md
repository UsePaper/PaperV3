A sentence carrying *emphasis*, **strong**, `code`, a [link](https://example.com) and an image ![alt](./i.png) all at once.

Marks that run together: ***both at once*** and then plain text.

A link holding `code` and *emphasis*: [see the `--json` *flag*](https://example.com).

Emphasis around punctuation: *"quoted and emphasised"* then plain.

A code span at the very start: `first` of the line.

**Strong at the very start** of the line, and *emphasis at the very end.*
